---
- name: Install MySQL Using Ansible Role
  hosts: allservers
  become: yes
  become_user: root
  roles:
  - { role: geerlingguy.mysql }
  tasks:
  - name: create a new database 
    mysql_db: >
      name=myflixdb 
      state=present 
      login_user=root
    tags:
    - mysql